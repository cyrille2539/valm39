'use client';

// Dans Next.js App Router, chaque navigation restaure le scroll automatiquement.
// Ce composant est conservé pour compatibilité mais ne fait rien.
const ScrollToTop = () => null;

export default ScrollToTop;
